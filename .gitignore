node_modules/
dist/
*.log

# Secrets — never commit
.env
.env.*
!.env.example

# Private keys — never commit
wallets/
keys.txt
*.key

# Telegram bot's encrypted wallet/settings store — never commit
data/

# Bundled fonts are needed at runtime for card rendering — keep them tracked.

# Leftovers from atomic writes (write to .tmp, then rename).
*.ts.tmp
